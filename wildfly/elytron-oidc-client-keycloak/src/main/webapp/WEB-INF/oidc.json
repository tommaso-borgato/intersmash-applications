{
    "realm": "${env.SSO_OIDC_KEYCLOAK_REALM}",
    "resource": "${env.SSO_OIDC_CLIENT_ID}",
    "auth-server-url" : "${env.SSO_OIDC_KEYCLOAK_URL}",
    "ssl-required" : "external",
    "disable-trust-manager": "true",
    "credentials": {
        "secret": "${env.SSO_OIDC_CLIENT_SECRET}"
    }
}
